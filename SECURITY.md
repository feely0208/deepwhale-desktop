# 安全说明（Security）

## API Key 处理原则

1. **绝不把 API Key 提交进代码仓库**（包括提交历史）。配置/密钥一律留在用户本机：
   - 环境变量 `DEEPSEEK_API_KEY`（启动 DSH 时由主进程注入）
   - 或应用内填写：Electron `safeStorage`（macOS/Windows 系统钥匙串、Linux 受支持时）加密后存入 `userData/settings.json` 的 `apiKeyEncrypted` 字段（base64）
2. **兜底存储**：当 `safeStorage` 不可用时，退化为可逆混淆（base64）存储，**不落明文**——这是非安全方案，仅保证不出现明文；优先使用系统安全存储。
3. **请求只在主进程发起**：余额查询等网络请求由主进程完成，渲染层（DSH 页面）只接收脱敏后的展示数据，永远拿不到 Key 明文。
4. **不打印、不显示**：Key 不出现在日志、面板、设置文件中（除加密字段外）。

## 报告漏洞

如果发现安全相关漏洞（如密钥泄漏路径、注入风险、权限问题），请**不要公开提交 Issue**，直接私信维护者或发送邮件，说明：

- 漏洞类型与影响
- 复现步骤
- 建议的修复方向

我们会在修复并发布后统一致谢。

## 构建发布

- 正式发布请配置代码签名（macOS Apple Developer ID + 公证、Windows 代码签名证书），避免未签名包带来的系统安全提示。
- CI 中的 `GH_TOKEN` 等仅使用仓库 Secrets，不写入代码。
