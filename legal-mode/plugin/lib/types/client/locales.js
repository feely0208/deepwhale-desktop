/* Locale namespace for the legal-mode plugin. The in-page workbench copy was
   removed with the overlay; the plugin now only drives the desktop deep link and
   renders nothing, so the dictionary keeps a single product-facing string. */
/** Chinese dictionary. */
export const zh = {
    'brand.legalMode': '法律模式',
};
/** English dictionary. */
export const en = {
    'brand.legalMode': 'Legal Mode',
};
/** Dictionaries registered under the plugin's locale namespace. */
export const dictionaries = { zh, en };
//# sourceMappingURL=locales.js.map